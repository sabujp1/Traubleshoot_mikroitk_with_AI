# Docker named volume data (auto-created by Docker, not needed in git)
loki-data/
grafana-data/

# loki-config.yaml is NOT mounted — Loki uses its built-in default config.
# This file is kept locally for reference only.
loki-config.yaml

# Sensitive MikroTik router config export — NEVER commit this
mikrotik_config_export.txt

# Temp / runtime files
*.log
positions.yaml
__pycache__/
*.pyc
*.pyo
.env

# OS files
.DS_Store
Thumbs.db
